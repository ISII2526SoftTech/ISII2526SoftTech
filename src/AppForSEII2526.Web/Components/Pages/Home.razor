@page "/"
@using AppForSEII2526.Web.API
@inject AppForSEII2526APIClient pruebaClient;

<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>

Welcome to your new app.

<br>
<br>
@if(_herramientas == null){

}
else foreach (var herramienta in _herramientas)
    {
        @herramienta.Nombre
        <br>
    }


@code {
    private ICollection<HerramientaDTO> _herramientas;

    protected override async Task OnInitializedAsync()
    {
        _herramientas = await pruebaClient.GetSelectFiltradoOfertaAsync(null,null);
    }
}